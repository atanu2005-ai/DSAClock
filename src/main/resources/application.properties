spring.application.name=DSAClock

# Oauth2
spring.security.oauth2.client.registration.google.client_id=335803627210-c474ssbqnp68rvr0jdia1r64299fdt4j.apps.googleusercontent.com
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.scope=profile, email
spring.security.oauth2.client.registration.google.redirect-uri={baseUrl}/login/oauth2/code/{registrationId}

# PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/dsaClock
spring.datasource.username=postgres
spring.datasource.password=Atanu@2005

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# PostgreSQL dialect
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect